spring:
  application:
    name: deltafi-ingress
  cloud:
    config:
      enabled: false
  main:
    allow-bean-definition-overriding: true

redis:
  url: http://localhost

ssl:
  key-store-password: not-set
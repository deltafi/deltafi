.clustermonitor:
  needs: []
  variables:
    PROJECT_NAME: deltafi-clustermonitor

"Publish deltafi-clustermonitor to GitLab 🐳":
  extends:
    - .clustermonitor
    - .docker

"Publish deltafi-clustermonitor to docker.io 🐳":
  extends:
    - .clustermonitor
    - .docker-multiarch

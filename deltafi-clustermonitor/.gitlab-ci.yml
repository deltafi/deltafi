.clustermonitor:
  needs: []
  variables:
    PROJECT_NAME: deltafi-clustermonitor

"🐳 Publish clustermonitor (GitLab)":
  extends:
    - .clustermonitor
    - .docker

"🐳 Publish clustermonitor (docker.io)":
  extends:
    - .clustermonitor
    - .docker-multiarch

query {
    actionFeed(
        action: "Utf8TransformAction"
    ) {
        did
        protocolStack {
            type
            objectReference {
                name
                bucket
                offset
                size
            }
        }
    }
}

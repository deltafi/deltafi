query {
    actionFeed(
        action: "SampleValidateAction"
    ) {
        did
        formattedData {
            formatAction
            objectReference {
                bucket
                name
                size
                offset
            }
        }
    }
}

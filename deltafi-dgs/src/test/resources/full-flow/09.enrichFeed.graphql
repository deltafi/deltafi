query {
    actionFeed(
        action: "SampleEnrichAction"
    ) {
        did
        domains {
            sampleDomain {
                did
                domained
            }
        }
    }
}

query {
    actionFeed(
        action: "SampleEgressAction"
    ) {
        did
        formattedData {
            formatAction
            objectReference {
                bucket
                name
                size
                offset
            }
        }
    }
}

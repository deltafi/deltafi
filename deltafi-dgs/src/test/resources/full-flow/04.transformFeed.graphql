query {
    actionFeed(
        action: "SampleTransformAction"
    ) {
        did
        protocolStack {
            type
            objectReference {
                name
                bucket
                offset
                size
            }
        }
    }
}

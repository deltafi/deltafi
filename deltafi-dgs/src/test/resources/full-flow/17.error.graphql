mutation {
    error(
        error: {
            originatorDid: "%1$s"
            fromAction: "AuthorityValidateAction"
            cause: "Authority XYZ not recognized"
            context: "Dead beef feed face cafe"
        }
    ) {
        did
        stage
        actions {
            name
            state
            errorCause
            errorContext
        }
    }
}
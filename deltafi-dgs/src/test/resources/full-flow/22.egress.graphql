mutation {
    egress(
        did: "%1$s"
        fromEgressAction: "SampleEgressAction"
    ) {
        did
        stage
        actions {
            name
            state
            errorMessage
        }
    }
}

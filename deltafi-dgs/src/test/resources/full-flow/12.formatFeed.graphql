query {
    actionFeed(
        action: "SampleFormatAction"
    ) {
        did
        domains {
            sampleDomain {
                did
                domained
            }
        }
        enrichment {
            sampleEnrichment {
                did
                enriched
            }
        }
    }
}

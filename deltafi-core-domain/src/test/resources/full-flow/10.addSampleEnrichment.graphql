mutation {
    addSampleEnrichment(
        did: "%1$s"
        enriched: true
    ) {
        did
        domains {
            key
            value
        }
        enrichment {
            key
            value
        }
    }
}

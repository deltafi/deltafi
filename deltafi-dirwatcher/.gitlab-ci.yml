.dirwatcher:
  needs: []
  variables:
    PROJECT_NAME: deltafi-dirwatcher

"Publish deltafi-dirwatcher to GitLab 🐳":
  extends:
    - .dirwatcher
    - .docker

"Publish deltafi-dirwatcher to docker.io 🐳":
  extends:
    - .dirwatcher
    - .docker-multiarch

.dirwatcher:
  needs: []
  variables:
    PROJECT_NAME: deltafi-dirwatcher

"🐳 Publish dirwatcher (GitLab)":
  extends:
    - .dirwatcher
    - .docker

"🐳 Publish dirwatcher (docker.io)":
  extends:
    - .dirwatcher
    - .docker-multiarch

query {
    actionFeed(
        action: "AuthorityValidateAction"
    ) {
        did
        sourceInfo {
            metadata {
                key
                value
            }
        }
    }
}

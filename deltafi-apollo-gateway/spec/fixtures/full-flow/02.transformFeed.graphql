query {
    actionFeed(
        action: "JsonUtf8StixTransformAction"
    ) {
        did
        protocolStack {
            type
            objectReference {
                name
                bucket
                offset
                size
            }
        }
    }
}

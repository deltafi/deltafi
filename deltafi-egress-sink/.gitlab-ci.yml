.egress-sink:
  needs: []
  variables:
    PROJECT_NAME: deltafi-egress-sink

"Publish deltafi-egress-sink to GitLab 🐳":
  extends:
    - .egress-sink
    - .docker

"Publish deltafi-egress-sink to docker.io 🐳":
  extends:
    - .egress-sink
    - .docker-multiarch

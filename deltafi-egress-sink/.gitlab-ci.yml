.egress-sink:
  needs: []
  variables:
    PROJECT_NAME: deltafi-egress-sink

"🐳 Publish egress-sink (GitLab)":
  extends:
    - .egress-sink
    - .docker

"🐳 Publish egress-sink (docker.io)":
  extends:
    - .egress-sink
    - .docker-multiarch

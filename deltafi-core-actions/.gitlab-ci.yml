.core-actions:
  variables:
    PROJECT_NAME: deltafi-core-actions

"🐳 Publish core-actions (GitLab)":
  extends:
    - .core-actions
    - .docker

"🐳 Publish core-actions (docker.io)":
  extends:
    - .core-actions
    - .docker-multiarch

apiVersion: apps/v1
kind: Deployment
metadata:
  name: deltafi-gateway
  labels:
    app: deltafi-gateway
spec:
  revisionHistoryLimit: 0
  selector:
    matchLabels:
      app: deltafi-gateway
  template:
    metadata:
      labels:
        app: deltafi-gateway
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/deployment-core-domain.yaml") . | sha256sum }}
    spec:
      containers:
      - name: deltafi-gateway
        image: {{ .Values.deltafi.gateway.image }}
        ports:
        - containerPort: 4000
      imagePullSecrets:
      - name: docker-secret
      serviceAccountName: deltafi-gateway

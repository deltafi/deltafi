apiVersion: apps/v1
kind: Deployment
metadata:
  name: deltafi-common-actions
  labels:
    app: deltafi-common-actions
spec:
  revisionHistoryLimit: 0
  selector:
    matchLabels:
      app: deltafi-common-actions
  template:
    metadata:
      labels:
        app: deltafi-common-actions
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap-actions.yaml") . | sha256sum }}
    spec:
      containers:
      - name: deltafi-common-actions
        image: "{{ .Values.deltafi.common_actions.image }}"
      imagePullSecrets:
      - name: docker-secret
      serviceAccountName: deltafi-actions

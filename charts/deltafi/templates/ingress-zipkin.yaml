apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: deltafi-zipkin
  annotations:
  {{- include "authAnnotations" . | nindent 4 }}
spec:
  {{- if eq .Values.ingress.tls.enabled true }}
  tls:
  - secretName: {{ coalesce .Values.ingress.tls.secrets.zipkin .Values.ingress.tls.secrets.default }}
  {{- end }}
  rules:
  - host: zipkin.{{ .Values.ingress.domain }}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: deltafi-zipkin
            port:
              number: 9411

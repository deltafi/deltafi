query {
    actionFeed(
        action: "StixAttackPatternLoadAction"
    ) {
        did
        protocolStack {
            type
            contentReference {
                name
                bucket
                offset
                size
            }
            metadata {
                key
                value
            }
        }
    }
}

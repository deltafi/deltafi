query {
    actionFeed(
        action: "JsonUtf8StixTransformAction"
    ) {
        did
        protocolStack {
            type
            contentReference {
                name
                bucket
                offset
                size
            }
        }
    }
}

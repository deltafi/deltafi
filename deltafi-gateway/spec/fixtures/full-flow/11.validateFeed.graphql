query {
    actionFeed(
        action: "Stix2.1ValidateAction"
    ) {
        did
        formattedData {
            formatAction
            contentReference {
                bucket
                name
                size
                offset
            }
        }
    }
}

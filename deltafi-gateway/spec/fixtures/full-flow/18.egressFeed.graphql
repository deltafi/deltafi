query {
    actionFeed(
        action: "StixEgressAction"
    ) {
        did
        formattedData {
            formatAction
            contentReference {
                bucket
                name
                size
                offset
            }
        }
    }
}

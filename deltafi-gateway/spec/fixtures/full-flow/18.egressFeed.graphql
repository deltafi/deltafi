query {
    actionFeed(
        action: "StixEgressAction"
    ) {
        did
        formattedData {
            formatAction
            objectReference {
                bucket
                name
                size
                offset
            }
        }
    }
}
